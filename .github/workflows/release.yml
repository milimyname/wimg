name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  check:
    runs-on: ubuntu-latest
    outputs:
      libwimg: ${{ steps.changes.outputs.libwimg }}
      web: ${{ steps.changes.outputs.web }}
      ios: ${{ steps.changes.outputs.ios }}
      sync: ${{ steps.changes.outputs.sync }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Detect changed paths
        id: changes
        run: |
          PREV_TAG=$(git tag -l --sort=-v:refname | sed -n '2p')
          if [ -n "$PREV_TAG" ]; then
            CHANGED=$(git diff --name-only "$PREV_TAG"..HEAD -- . ':(exclude)CHANGELOG.md' ':(exclude)wimg-web/package.json' ':(exclude)wimg-ios/project.yml')
          else
            CHANGED=$(git diff --name-only HEAD~1 -- . ':(exclude)CHANGELOG.md' ':(exclude)wimg-web/package.json' ':(exclude)wimg-ios/project.yml')
          fi
          echo "libwimg=$(echo "$CHANGED" | grep -q '^libwimg/' && echo true || echo false)" >> $GITHUB_OUTPUT
          echo "web=$(echo "$CHANGED" | grep -q '^wimg-web/' && echo true || echo false)" >> $GITHUB_OUTPUT
          echo "ios=$(echo "$CHANGED" | grep -q '^wimg-ios/' && echo true || echo false)" >> $GITHUB_OUTPUT
          echo "sync=$(echo "$CHANGED" | grep -q '^wimg-sync/' && echo true || echo false)" >> $GITHUB_OUTPUT

      - uses: mlugg/setup-zig@v2
        with:
          version: 0.15.2

      - uses: oven-sh/setup-bun@v2

      - name: Download SQLite amalgamation
        working-directory: libwimg/vendor
        run: |
          curl -fsSL https://www.sqlite.org/2026/sqlite-amalgamation-3520000.zip -o sqlite.zip
          unzip -o sqlite.zip
          cp sqlite-amalgamation-3520000/sqlite3.c sqlite3.c
          cp sqlite-amalgamation-3520000/sqlite3.h sqlite3.h

      - name: Zig fmt check
        working-directory: libwimg
        run: zig fmt --check src/

      - name: Zig tests
        working-directory: libwimg
        run: zig build test

      - name: Install web dependencies
        working-directory: wimg-web
        run: bun install --frozen-lockfile

      - name: oxfmt check
        working-directory: wimg-web
        run: bunx oxfmt --check

      - name: oxlint
        working-directory: wimg-web
        run: bunx oxlint

      - name: bun test
        working-directory: wimg-web
        run: bun test

      - name: svelte-check
        working-directory: wimg-web
        run: bun run check

      - name: Install sync dependencies
        working-directory: wimg-sync
        run: bun install --frozen-lockfile

      - name: Typecheck sync worker
        working-directory: wimg-sync
        run: bunx tsc --noEmit

  build-and-deploy-web:
    needs: check
    if: needs.check.outputs.web == 'true' || needs.check.outputs.libwimg == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: mlugg/setup-zig@v2
        with:
          version: 0.15.2

      - uses: oven-sh/setup-bun@v2

      - name: Download SQLite amalgamation
        working-directory: libwimg/vendor
        run: |
          curl -fsSL https://www.sqlite.org/2026/sqlite-amalgamation-3520000.zip -o sqlite.zip
          unzip -o sqlite.zip
          cp sqlite-amalgamation-3520000/sqlite3.c sqlite3.c
          cp sqlite-amalgamation-3520000/sqlite3.h sqlite3.h

      - name: Build WASM
        run: ./scripts/build-wasm.sh

      - name: Upload WASM artifacts
        uses: actions/upload-artifact@v7
        with:
          name: libwimg-wasm
          path: |
            wimg-web/static/libwimg.wasm
            wimg-sync/libwimg-compact.wasm

      - name: Install web dependencies
        working-directory: wimg-web
        run: bun install --frozen-lockfile

      - name: Build web
        working-directory: wimg-web
        run: bun run build

      - name: Deploy to Cloudflare Pages
        working-directory: wimg-web
        run: bunx wrangler pages deploy .svelte-kit/cloudflare --project-name wimg-web --branch main
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

  build-ios:
    needs: check
    if: needs.check.outputs.ios == 'true' || needs.check.outputs.libwimg == 'true'
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6

      - uses: mlugg/setup-zig@v2
        with:
          version: 0.15.2

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Download SQLite amalgamation
        working-directory: libwimg/vendor
        run: |
          curl -fsSL https://www.sqlite.org/2026/sqlite-amalgamation-3520000.zip -o sqlite.zip
          unzip -o sqlite.zip
          cp sqlite-amalgamation-3520000/sqlite3.c sqlite3.c
          cp sqlite-amalgamation-3520000/sqlite3.h sqlite3.h

      - name: Build iOS XCFramework
        run: ./scripts/build-ios.sh

      - name: Generate Xcode project
        run: ./scripts/gen-xcodeproj.sh

      - name: Zip XCFramework
        working-directory: libwimg/build-ios
        run: zip -r libwimg.xcframework.zip libwimg.xcframework

      - name: Upload iOS artifact
        uses: actions/upload-artifact@v7
        with:
          name: libwimg-ios
          path: libwimg/build-ios/libwimg.xcframework.zip

      # TODO: Re-enable when Apple signing certs are configured in CI.
      # Upload manually via Xcode for now (see .claude/rules/testflight.md).
      #
      # - name: Archive for TestFlight
      #   run: |
      #     xcodebuild archive \
      #       -project wimg-ios/wimg.xcodeproj \
      #       -scheme wimg \
      #       -destination "generic/platform=iOS" \
      #       -archivePath build/wimg.xcarchive \
      #       -allowProvisioningUpdates \
      #       -quiet
      #
      # - name: Export IPA
      #   run: |
      #     cat > build/ExportOptions.plist <<PLIST
      #     <?xml version="1.0" encoding="UTF-8"?>
      #     <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
      #     <plist version="1.0">
      #     <dict>
      #         <key>method</key>
      #         <string>app-store-connect</string>
      #         <key>destination</key>
      #         <string>upload</string>
      #         <key>signingStyle</key>
      #         <string>automatic</string>
      #         <key>teamID</key>
      #         <string>W5N395VNYH</string>
      #     </dict>
      #     </plist>
      #     PLIST
      #     xcodebuild -exportArchive \
      #       -archivePath build/wimg.xcarchive \
      #       -exportOptionsPlist build/ExportOptions.plist \
      #       -exportPath build/export \
      #       -allowProvisioningUpdates \
      #       -quiet
      #
      # - name: Upload to TestFlight
      #   env:
      #     ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
      #     ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}
      #     ASC_KEY_CONTENT: ${{ secrets.ASC_KEY_CONTENT }}
      #   run: |
      #     mkdir -p ~/.appstoreconnect
      #     echo "$ASC_KEY_CONTENT" > ~/.appstoreconnect/key.p8
      #     xcrun altool --upload-app \
      #       --type ios \
      #       --file build/export/wimg.ipa \
      #       --apiKey "$ASC_KEY_ID" \
      #       --apiIssuer "$ASC_ISSUER_ID"

  deploy-sync:
    needs: [check, build-and-deploy-web]
    if: |
      always() && !failure() && !cancelled() &&
      needs.build-and-deploy-web.result == 'success' &&
      (needs.check.outputs.sync == 'true' || needs.check.outputs.libwimg == 'true')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: oven-sh/setup-bun@v2

      - name: Download WASM artifacts
        uses: actions/download-artifact@v8
        with:
          name: libwimg-wasm
          path: .

      - name: Install dependencies
        working-directory: wimg-sync
        run: bun install --frozen-lockfile

      - name: Deploy Worker
        working-directory: wimg-sync
        run: bunx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

  release:
    needs: [check, build-and-deploy-web, build-ios]
    if: always() && !failure() && !cancelled()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Download WASM artifacts
        if: needs.build-and-deploy-web.result == 'success'
        uses: actions/download-artifact@v8
        with:
          name: libwimg-wasm
          path: artifacts/libwimg-wasm

      - name: Download iOS artifacts
        if: needs.build-ios.result == 'success'
        uses: actions/download-artifact@v8
        with:
          name: libwimg-ios
          path: artifacts/libwimg-ios

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG=${GITHUB_REF#refs/tags/}

          # Find the last tag that has a GitHub release (skips failed CI tags)
          PREV_TAG=""
          for t in $(git tag -l --sort=-v:refname | tail -n +2); do
            if gh release view "$t" --json tagName >/dev/null 2>&1; then
              PREV_TAG="$t"
              break
            fi
          done

          if [ -n "$PREV_TAG" ]; then
            NOTES=$(git log --pretty=format:"- %s" "$PREV_TAG"..HEAD \
              | grep -vE '^- (release|chore|ci|build): ' || echo "- Version bump")
          else
            NOTES=$(git log --pretty=format:"- %s" HEAD~20..HEAD \
              | grep -vE '^- (release|chore|ci|build): ' || echo "- Version bump")
          fi

          echo "$NOTES" > release-notes.md

          # Collect available artifacts
          ASSETS=()
          [ -f artifacts/libwimg-wasm/wimg-web/static/libwimg.wasm ] && ASSETS+=(artifacts/libwimg-wasm/wimg-web/static/libwimg.wasm)
          [ -f artifacts/libwimg-ios/libwimg.xcframework.zip ] && ASSETS+=(artifacts/libwimg-ios/libwimg.xcframework.zip)

          gh release create "$TAG" \
            --title "$TAG" \
            --notes-file release-notes.md \
            "${ASSETS[@]}"
